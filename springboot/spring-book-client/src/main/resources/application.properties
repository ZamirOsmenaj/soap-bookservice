# Server host — overridden at runtime via -Dserver.host=... (or env var)
# Default points to the Docker service name used in docker-compose
server.host=spring-book-server

# Disable the web server — this is a one-shot client, not a service
spring.main.web-application-type=none
